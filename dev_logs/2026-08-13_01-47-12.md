Task: Update theme colors based on palette.

Plan:
1. Update src/theme/index.ts with the new primary (#85A183), secondary (#4B654A), tertiary (#D9E0D8), and neutral (#777774) colors.
2. Verify the changes using typescript compilation.

Actions:
- Updated `src/theme/index.ts` to reflect the requested colors.
- Verified compilation with `npx typescript@latest/tsc --noEmit`.
