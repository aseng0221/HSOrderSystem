# Task: Change Payment Strategy to Receipt Upload & Fixed Top-Up Packages

## Request
- Keep Fiuu, but change the strategy.
- Drink orders allow only Option 1 (Manual Transfer / Receipt Upload) and In-App Wallet.
- For In-App Wallet top-up, set fixed packages: RM30 (RM3 bonus), RM50 (RM6 bonus), and RM100 (RM15 bonus) via Fiuu.

## Plan
1. Fix Metro Bundler crash (Invalid Image Asset).
2. Restrict Payment Methods in Cart (keep only NextDoor Balance and Manual Transfer).
3. Implement Wallet Top-Up Packages in AccountScreen (replace text input with package buttons).
4. Verify updates, run type checks, and run linting.
5. Complete Pre-Commit Steps.

## Actions & Updates
- Added `@react-native-firebase/storage` and `react-native-image-picker` to dependencies.
- Updated `Order` type to include `receiptUrl`, `pending_verification` status, and `manual_transfer` payment method.
- Added `uploadReceiptToStorage` helper in `src/services/storage.ts`.
- Implemented TNG QR and receipt upload logic in `src/screens/CartScreen.tsx` with a `qrModalVisible` state and a fallback UI for the QR image to avoid Metro errors.
- Restricted payment methods in `CartScreen` to only `nextdoor_balance` and `manual_transfer`.
- Replaced the arbitrary `TextInput` for top-up in `src/screens/AccountScreen.tsx` with defined buttons for RM30, RM50, and RM100.
- Updated the logic to correctly use the package `price` for the payment gateway (Fiuu) and add the combined `price + bonus` to the user's wallet upon success.
- Verified functionality with `npm test`, `eslint --fix`, and `tsc --noEmit`.
- Confirmed changes with `git diff`.
- Code review performed and learnings successfully recorded into memory.
