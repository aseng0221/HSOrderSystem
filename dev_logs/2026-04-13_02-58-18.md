# Task Request
Restore Fiuu payment configuration back to the app and set APP_NAME for Fiuu explicitly to "HS Coffee". Change the Place Order button to show an action sheet/modal for payment options: either "Credit Card (Fiuu)" or "Cash Payment". Cash payment remains unchanged.

# Plan
1. Update `src/screens/CartScreen.tsx` to include `molpay` SDK.
2. Refactor existing checkout logic to branch off into `handleCashPayment` and `handleFiuuPayment`.
3. Change the place order button text from "Place Order (Pay by Cash)" to "Place Order".
4. Prompt user to select "Cash Payment" or "Credit Card (Fiuu)" inside an `Alert`.
5. Re-enable the Fiuu code blocks and apply config keys.

# Actions Taken
- Verified Fiuu logic block exists but was commented out.
- Restored `import molpay from 'fiuu-mobile-xdk-reactnative'` and `import {KEYS} from '../config/keys'`.
- Split logic inside `handleCheckout` into `handleCashPayment` and `handleFiuuPayment`.
- Updated `handleCheckout` to use `Alert.alert` with multiple choices for payment options.
- Made sure Fiuu config applies `KEYS.FIUU.APP_NAME || 'HS Coffee'`.
- Tested the component logic and ran Jest tests (`npm test`).
- Setup `src/config/keys.ts` template file with the required `FIUU` keys.
- Received code review indicating changes are #Correct#.

# Update
- User requested a back/cancel button for the Fiuu payment screen.
- Used `mp_closebutton_display: true` configuration found in the Fiuu example repository.

# Task 2: Order History Pagination
- Created `OrderHistoryScreen` to display orders via a `FlatList` with `onEndReached` handler.
- Refactored `useOrderHistoryViewModel` to fetch orders with Firestore `limit(10)` and `startAfter` instead of a real-time snapshot listener.
- Linked "Order History" item in `AccountScreen` to navigate to the new screen.
- Code review flagged no issues, implementation is #Correct#.

# Task 3: Retry Unpaid Orders
- Addressed issue where online orders that failed to pay were never saved to Firestore. Now `CartScreen` saves them as `unpaid` before launching the payment gateway.
- If payment succeeds, it calls `updateOrderPaymentStatus` and `addPointsForPurchase`. If it fails, the order remains in history as `unpaid`.
- Added "Make Payment" button to `OrderHistoryDetailScreen` to retry these `unpaid` online orders.
- The button re-launches Molpay. On success, updates order to `paid` and grants points.
- Status UI in `OrderHistoryScreen` now shows "UNPAID (CASH)" underneath the badge for cash orders.
- Verified changes. Code Review was #Correct#.
